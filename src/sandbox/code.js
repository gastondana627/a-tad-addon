import addOnUISdk from "https://new.express.adobe.com/static/add-on-sdk/sdk.js";

addOnUISdk.ready.then(() => {
    console.log("Document Sandbox is ready and connected!");

    // We will add code here later to create shapes, text,
    // and interact with the user's document.
});